/**
 * Dart 变量：
 *  dart是一个强大的脚本类语言，可以不预先定义变量类型，自动会类型推断
 * 
 *  dart中定义的变量可以通过var关键字，代替类型来申明变量类型
 * 
 * Dart的命名规则：
 *  1.变量名称必须由数字、字母、下划线和美元符($)组成。
 *  2.注意：标识符开头不能是数字。
 *  3.标识符不能是保留字和关键字
 *  4.变量的名字是区分大小写的，如：age 和 Age 是不同变量。
 *    在实际的运用中，也不建议这样用
 *  5.标识符一定要见名思意
 * 
 * Dart 常量： final 和 const 修饰符
 *  const值不变 一开始就必须赋值
 *  final可以开始不赋值，但是一旦赋值之后就不能被修改
 * 
 */